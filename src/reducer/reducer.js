export const albumReducer = (state, action)=>{
    switch (action.type) {
        case "ALBUM_ID":
            return state
            break;
    
        default:
            return state
            break;
    }
}